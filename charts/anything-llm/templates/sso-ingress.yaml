{{- if and .Values.sso.enabled .Values.sso.ingress.enabled -}}
{{- $fullName := include "anything-llm.fullname" . -}}
{{- $svcPort := .Values.sso.service.port -}}
{{- if and .Values.sso.ingress.className (not (hasKey .Values.sso.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.sso.ingress.annotations "kubernetes.io/ingress.class" .Values.sso.ingress.className}}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-sso
  labels:
    {{- include "anything-llm.labels" . | nindent 4 }}
    app.kubernetes.io/component: sso
  {{- with .Values.sso.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.sso.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.sso.ingress.className }}
  {{- end }}
  {{- if .Values.sso.ingress.tls }}
  tls:
    {{- range .Values.sso.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.sso.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-sso
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-sso
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }} 